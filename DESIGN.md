# Product design identity

<!--
  코드 조사 결과를 CONFIRMED(코드에 구현됨) / MISSING(없음) / PROPOSED(제안, 미구현)로
  표시했다. 조사 대상: index.html의 :root 토큰과 CSS 전체, src/main.ts의 상태 표시 로직.
-->

## Purpose

혼자 체스를 두고 싶은 사람이 설명 없이 바로 한 판 시작하는 것. 계정도, 설정도, 튜토리얼도
없다. 화면에서 가장 중요한 것은 언제나 반상(盤上)이며 나머지 UI는 그 아래로 물러난다.

## Design principles

1. **반상이 화면을 지배한다.** 보드는 `min(88vw, 88vh, 560px)`로 가용 공간을 최대한 쓰고,
   컨트롤은 보드보다 작고 어둡다.
2. **말은 전통 체스 표기를 따른다.** 유니코드 기물(♔♕♖♗♘♙)에 커스텀 그래픽을 얹지 않는다.
3. **상태는 반상 위에서 말한다.** 선택·합법수·직전 수·체크는 텍스트가 아니라 칸 자체의
   표시로 알린다. 텍스트 상태줄은 보조 수단이다.
4. **잘못 클릭해도 아무 일도 일어나지 않는다.** 불법 수는 오류를 띄우지 않고 선택만 갱신한다.
5. **한글 UI, 체스 용어는 관례를 따른다.** "백/흑", "체크", "체크메이트", "무승부".
6. **전투는 결과만 통보하지 않고 과정을 보여준다.** 결투 모드에서 잡기의 승패는 확률이므로,
   플레이어가 납득하려면 타격 하나하나와 남은 HP가 눈에 보여야 한다. 판정은 이미 끝난 뒤
   재생하는 것이며, 화면은 판정을 바꾸지 않는다.
7. **거짓말하지 않는다.** 전투에 져서 무산된 공격은 기보에 `✗`로 남긴다. SAN 표기상으로는
   잡은 것처럼 보이기 때문이다.

## Color palette

역할 기반. 실제 값은 `index.html`의 `:root` (CONFIRMED).

| 역할 | 토큰 | 용도 |
| --- | --- | --- |
| 밝은 칸 | `--light` | 보드 밝은 칸 |
| 어두운 칸 | `--dark` | 보드 어두운 칸 |
| 배경 | `--bg` | 페이지 바탕 |
| 표면 | `--panel` | 버튼·셀렉트·기보 패널 |
| 본문 | `--text` | 모든 텍스트 |
| 강조 | `--accent` | 선택된 칸 테두리, 컨트롤 hover, HP 바 |
| 피해 | `--danger` | 전투 피해량, 위험 HP, 무산된 수(`✗`) |
| 치명타 | `--crit` | 치명타 피해량 |

토큰 외 색은 보드 위 오버레이에만 쓴다: 직전 수 노란 틴트, 체크 붉은 방사형, 합법수 회색 점.
이들은 반투명 값이라 밝은 칸·어두운 칸 양쪽에서 동작해야 하므로 토큰화하지 않는다.

## Typography

시스템 폰트 스택 (`system-ui`, `Apple SD Gothic Neo`), 본문 15px/1.5. 제목만
22px·`letter-spacing: .06em`. 기물은 `min(9vw, 9vh, 54px)`로 칸 크기를 따라간다. (CONFIRMED)

## Shape and elevation

모든 모서리 6px. 그림자는 보드에 하나만 (`0 8px 32px #0008`) — 보드가 떠 있고 나머지는
바탕에 붙어 있다는 위계. (CONFIRMED)

## Layout and responsiveness

세로 단일 컬럼 중앙 정렬. 보드는 `aspect-ratio: 1` + 뷰포트 기반 크기라 별도 breakpoint가
없다. 컨트롤 줄은 `flex-wrap`으로 좁은 화면에서 접힌다. (CONFIRMED)

64칸은 언제나 정사각형이고 크기가 같다. 그리드의 행·열을 모두 `1fr`로 못 박아야 한다 —
열만 선언하면 행 높이가 내용을 따라가서 기물 없는 가로줄이 얇아진다. (CONFIRMED)

## Interaction states

| 상태 | 구현 |
| --- | --- |
| hover | CONFIRMED — 버튼·셀렉트 테두리가 `--accent`로 |
| active | CONFIRMED — 선택된 칸에 `--accent` inset 4px |
| loading | CONFIRMED — AI 탐색 중 "AI가 고민 중…", 전투 재생 중 "전투 중…". 둘 다 입력을 잠근다 |
| error | 해당 없음 — 불법 수는 오류가 아니라 무시(원칙 4) |
| empty | 해당 없음 — 빈 상태가 존재하지 않는다(항상 초기 배치로 시작) |
| success | CONFIRMED — "체크메이트 — 승리" 상태줄 |
| disabled | CONFIRMED — AI 탐색 중 컨트롤에 `disabled` + `opacity: .45` |
| focus | CONFIRMED — `:focus-visible`에 `--accent` 2px 아웃라인. 단 보드 칸은 tabbable이 아니라 적용 대상이 아니다 |

## Components

### 결투 오버레이 (`#duel`)

잡기가 발생하면 반상을 어둡게 덮고 두 기물을 마주 세운다.

- 기물은 반상과 같은 유니코드 표기를 쓰되 `--light` 원판 위에 올린다. 어두운 패널 위에
  그냥 두면 흑 기물이 보이지 않는다 (실제로 그렇게 만들었다가 고쳤다).
- 이름 아래 `HP · 공격` 수치를 노출한다. 승패가 확률인 이상 플레이어가 판단 근거를
  가질 수 있어야 하고, 별도 도움말 화면보다 여기가 배우기 쉽다.
- 타격마다 맞은 쪽이 흔들리고 피해량이 떠오르며 HP 바가 줄어든다. 치명타는 `--crit`에
  더 큰 글자.
- 총 재생 시간은 타격 수와 무관하게 약 2.6초를 넘지 않는다 (간격을 나눠 쓴다).

## Accessibility

- MISSING — 기물 칸에 `aria-label`이 없다. 스크린 리더로는 반상을 읽을 수 없다.
- MISSING — 키보드만으로 수를 둘 수 없다.
- 색 대비: 미검증. 밝은 칸 위 흰 기물은 `text-shadow` 외곽선에 의존한다.

접근성은 현재 이 프로젝트의 우선순위가 아니다. 손대기로 결정하면 위 세 항목을 한 작업으로
묶는다.

## Do not

- 보드 크기를 고정 px로 바꾸지 않는다.
- 상태를 alert·confirm으로 알리지 않는다(브라우저 모달은 게임 흐름을 끊는다).
- 기물을 이미지 스프라이트로 교체하지 않는다. 유니코드로 충분하다.
- 전투 재생을 건너뛸 수 없게 만들지 않는다. 지금은 건너뛰기가 없지만, 넣는다면 재생만
  생략하고 판정은 그대로여야 한다 — 다시 굴리기는 절대 아니다.
- 전투 결과를 화면에서 계산하지 않는다. `combat.ts`가 낸 판정을 재생만 한다.
