# Product design identity

<!--
  코드 조사 결과를 CONFIRMED(코드에 구현됨) / MISSING(없음) / PROPOSED(제안, 미구현)로
  표시했다. 조사 대상: index.html의 :root 토큰과 CSS 전체, src/main.ts의 상태 표시 로직.
-->

## Purpose

혼자 체스를 두고 싶은 사람이 설명 없이 바로 한 판 시작하는 것. 계정도, 설정도, 튜토리얼도
없다. 화면에서 가장 중요한 것은 언제나 반상(盤上)이며 나머지 UI는 그 아래로 물러난다.

## Design principles

1. **반상이 화면을 지배한다.** 보드는 `min(92vw, 1000px, 82vh)`로 가용 공간을 최대한 쓰고,
   컨트롤은 보드보다 작고 어둡다.
2. **기물은 캐릭터다.** 산 자(모험가)와 죽은 자(해골)가 싸운다. 진영은 색 보정이 아니라
   실루엣으로 갈리고, 여섯 역은 무기와 머리 장식으로 갈린다 — 위에서 봐도 구분되어야 한다.
3. **상태는 반상 위에서 말한다.** 선택·합법수·직전 수·체크는 텍스트가 아니라 칸 위의
   표시로 알린다. 텍스트 상태줄은 보조 수단이다.
4. **잘못 클릭해도 아무 일도 일어나지 않는다.** 불법 수는 오류를 띄우지 않고 선택만 갱신한다.
5. **한글 UI, 체스 용어는 관례를 따른다.** "백/흑", "체크", "체크메이트", "무승부".
6. **전투는 결과만 통보하지 않고 과정을 보여준다.** 결투 모드에서 잡기의 승패는 확률이므로,
   플레이어가 납득하려면 타격 하나하나와 남은 HP가 눈에 보여야 한다. 판정은 이미 끝난 뒤
   재생하는 것이며, 화면은 판정을 바꾸지 않는다.
7. **거짓말하지 않는다.** 전투에 져서 무산된 공격은 기보에 `✗`로 남긴다. SAN 표기상으로는
   잡은 것처럼 보이기 때문이다.

## Color palette

역할 기반. 실제 값은 `index.html`의 `:root` (CONFIRMED).

| 역할 | 토큰 | 용도 |
| --- | --- | --- |
| 밝은 칸 | `--light` | 보드 밝은 칸 (three.js 재질에도 같은 값) |
| 어두운 칸 | `--dark` | 보드 어두운 칸 (three.js 재질에도 같은 값) |
| 배경 | `--bg` | 페이지 바탕 |
| 표면 | `--panel` | 버튼·셀렉트·기보 패널 |
| 본문 | `--text` | 모든 텍스트 |
| 강조 | `--accent` | 선택된 칸 고리, 컨트롤 hover, HP 바 |
| 피해 | `--danger` | 전투 피해량, 위험 HP, 무산된 수(`✗`) |
| 치명타 | `--crit` | 치명타 피해량 |

토큰 외 색은 반상 위 표시에만 쓴다: 직전 수 노란 틴트, 체크 붉은 틴트, 합법수 회색 점·고리.
반투명 값이라 밝은 칸·어두운 칸 양쪽에서 동작해야 하므로 토큰화하지 않는다.
기물 색은 모델 원본 그대로다 — 물들이지 않는다.

## Typography

시스템 폰트 스택 (`system-ui`, `Apple SD Gothic Neo`), 본문 15px/1.5. 제목만
22px·`letter-spacing: .06em`. (CONFIRMED)

## Shape and elevation

모든 모서리 6px. 그림자는 보드에 하나만 (`0 8px 32px #0008`) — 보드가 떠 있고 나머지는
바탕에 붙어 있다는 위계. (CONFIRMED)

## Layout and responsiveness

세로 단일 컬럼 중앙 정렬. 반상은 `width: min(92vw, 1000px, 82vh)` + `aspect-ratio: 1.32`라
별도 breakpoint가 없다. 컨트롤 줄은 `flex-wrap`으로 좁은 화면에서 접힌다. (CONFIRMED)

**`82vh` 항목이 세로 제약이다.** 가로만 걸면 제목·상태줄·컨트롤·기보를 더한 페이지가 화면을
넘겨 반상 아래쪽이 잘린다. 3D 는 스크롤로 만회할 수 없다 — 잘린 칸은 클릭도 안 된다. (CONFIRMED)

## Interaction states

| 상태 | 구현 |
| --- | --- |
| hover | CONFIRMED — 버튼·셀렉트 테두리가 `--accent`로 |
| active | CONFIRMED — 선택된 기물 발밑에 `--accent` 고리 |
| loading | CONFIRMED — AI 탐색 중 "AI가 고민 중…", 전투 재생 중 "전투 중…". 둘 다 입력을 잠근다 |
| error | 해당 없음 — 불법 수는 오류가 아니라 무시(원칙 4) |
| empty | 해당 없음 — 빈 상태가 존재하지 않는다(항상 초기 배치로 시작) |
| success | CONFIRMED — "체크메이트 — 승리" 상태줄 |
| disabled | CONFIRMED — AI 탐색·전투 중 컨트롤에 `disabled` + `opacity: .45` |
| focus | CONFIRMED — `:focus-visible`에 `--accent` 2px 아웃라인. 반상은 canvas 하나라 칸 단위 포커스가 없다 |

## Components

### 반상 (`#board`, three.js)

원근 카메라로 자기 진영 뒤에서 내려다본다. 흑을 잡으면 카메라가 반대편으로 넘어간다 —
반상을 돌리지 않는다.

- 기물 키는 역할 서열을 따른다: 폰 0.88 → 킹 1.34 (칸 한 변이 1). 배율은 **몸 기준**으로
  잰다. 무기를 먼저 붙이면 지팡이 하나가 키를 좌우한다.
- 진영은 발밑 원판(`#f4f1ea` / `#211d1a`)으로도 한 번 더 표시한다. 위에서 보면 실루엣만으로는
  소유가 덜 분명하다.
- 클릭은 기물이 아니라 **바닥 평면**을 맞힌다. 기물에 가려진 칸도 집을 수 있어야 한다.
- **표시는 칸 전체를 물들인다.** 2D 판에서 쓰던 가운데 점은 3D 에서 안 통한다 — 원근 때문에
  기물이 자기 바로 뒤 칸의 화면 자리를 통째로 덮어서, 킹을 고르면 갈 수 있는 칸의 점이
  투구 위에 찍힌다. `depthTest` 를 꺼도 마찬가지다. 칸을 통째로 칠하면 기물 좌우로
  삐져나온 부분이 남아 가려져도 읽힌다.
- **갈 수 있으면 초록, 못 가면 빨강.** 규칙이 막는 칸(킹이 겨눠지는 자리, 핀에 걸린 기물,
  체크를 못 푸는 수)은 빨간 칸 + X 로 표시하고, 눌렀을 때 상태줄에 이유를 한 줄 붙인다.
  아무 표시 없이 클릭이 먹통이면 규칙이 아니라 고장으로 읽힌다.
- 조용한 수는 240ms 미끄러진다. 순간이동하면 3D 에서는 고장난 것처럼 보인다.
- **공격자는 수비 기물 앞까지 걸어가서 싸운다.** 원래 칸에 세워 두면 룩이 반상 끝에서
  허공에 칼을 휘두르고, 멀수록 둘이 화면 양끝으로 벌어져 누가 싸우는지 안 보인다.
- **직전 수는 화살표로 그린다.** 칸 두 개만 물들이면 "어디서 어디로" 가 안 읽히고, 기물이
  빽빽하면 어느 기물이 움직였는지도 모른다. 화살표는 도착 칸 **앞에서 끊는다** — 한가운데까지
  그리면 그 자리에 선 기물이 화살촉을 가린다. 전투에 져서 무산된 수는 빨간 화살표다.
- **두 진영은 언제나 서로를 마주 본다.** 모델의 정면이 +z 라 회전이 필요한 쪽은 백이다.
  회전을 아무에게도 주지 않으면 양쪽이 나란히 바깥을 보고 선다 — 3D 에서 "기본값"은
  중립이 아니라 하나의 선택이다. 전투로 틀어 둔 각도는 매 `sync()` 에서 되돌린다.

### 전투 계기판 (`#duel`)

전투 자체는 반상 위에서 벌어진다 — 카메라가 두 기물로 밀고 들어가고, 타격마다 모델의
`Attack` 과 `Hit`, 진 쪽은 `Death` 가 재생된다. 계기판은 3D 로 읽기 어려운 것만 맡는다.

- 이름 옆에 최대 HP 를 적는다. 승패가 확률인 이상 플레이어가 판단 근거를 가져야 하고,
  별도 도움말 화면보다 여기가 배우기 쉽다.
- 타격마다 피해량이 떠오르고 HP 바가 줄어든다. 치명타는 `--crit`.
- 계기판은 반상을 덮지 않는다. 덮으면 정작 보여주려던 싸움이 가려진다.
- **기보의 `✗` 는 전투가 끝난 뒤에 붙인다.** 먼저 붙이면 결과를 미리 알려 준다.
- 체력이 전투 사이에 이어지므로 계기판은 `현재/최대` 를 함께 보여준다. 만피에서 시작하지
  않는 전투가 기본이다.

### 아이템

빈 칸 위에 떠서 도는 결정체 + 바닥의 같은 색 고리. 정적인 반상에서 "이건 다른 것"으로
읽히게 움직임을 준다.

- **색만으로는 효과를 알 수 없다.** 반상 아래 안내줄에 놓인 아이템의 칸·이름·효과를 적는다.
  아이템이 없으면 줄 자체가 사라진다.
- 능력을 지닌 기물은 발밑에 보라 고리. 어느 기물이 특별한지 반상에서 보여야 한다.
  고리는 기물 받침(반지름 0.4)보다 **바깥**에 그린다 — 안쪽에 그리면 받침에 덮여 안 보인다.
- 주우면 상태줄에 이름과 효과가 한 줄 뜬다.

### 연승 런 (`#run`, `#reward`)

상태줄이 "지금 이 판"이라면 `#run` 은 "지금 이 런"이다 — 연승·다음 상대·가진 보상. 판이
바뀌어도 남는 것들이라 반상 밖에 계속 떠 있는다. 보상은 `--buff`, 연승 수는 `--accent`.

- **보상 화면은 판이 끝난 뒤에만 반상을 덮는다.** 그 순간에는 반상에 더 볼 것이 없다.
  브라우저 모달(`confirm`)은 쓰지 않는다 — 게임 흐름을 끊고 스타일도 못 입힌다.
- **마지막 수가 눈에 남을 틈을 준다** (0.7초). 결정타 직후 화면이 덮이면 뭘 맞았는지 모른다.
- 카드는 버튼이다. 첫 카드에 포커스가 가므로 키보드로도 고를 수 있다.
- 패배도 같은 화면을 쓴다. 카드가 하나(다시 시작)로 줄 뿐이다 — 끝과 시작이 한 자리에 있다.
- 무승부도 같은 화면인데 카드가 "다음 판"이다. 런이 안 끊기는 것을 카드 이름으로 알린다.
- **무승부는 이유를 규칙 이름으로 적는다.** "이겼는데 무승부" 로 보이는 순간이라
  (스테일메이트는 이기고 있을 때 더 잘 난다) 한 단어로 끝내면 버그로 읽힌다.
- **해금은 패배 화면에서 알린다.** 새 카드가 열린 것은 다음 런을 시작할 이유라서, 런이
  끝나는 그 화면에 붙어 있어야 한다. 별도 화면을 만들지 않는다.
- 합성은 카드에 `폰 → 나이트` 처럼 화살표로 쓴다. 무엇이 무엇으로 바뀌는지가 전부다.

### 체력 바

다친 기물 머리 위에만 뜬다. 32개가 전부 떠 있으면 반상이 계기판이 된다 — 만피는 표시하지
않는 것이 규칙이다. 30% 이하면 빨강으로 바뀐다. 카메라를 향해 매 프레임 세운다.

## Accessibility

- MISSING — 반상이 canvas 하나라 스크린 리더로는 아무것도 읽을 수 없다. 2D 시절보다 나빠졌다.
- MISSING — 키보드만으로 수를 둘 수 없다.
- MISSING — WebGL 이 없으면 아무것도 안 나온다. 대체 화면이 없다.
- 색 대비: 미검증.

접근성은 현재 이 프로젝트의 우선순위가 아니다. 손대기로 결정하면 위 항목을 한 작업으로
묶는다 — canvas 옆에 숨은 표(table)로 반상을 복제하는 것이 가장 값싼 출발점이다.

## Do not

- 보드 크기를 고정 px로 바꾸지 않는다.
- 상태를 alert·confirm으로 알리지 않는다(브라우저 모달은 게임 흐름을 끊는다).
- 머리를 장비로 분류하지 않는다. 모델의 장비 메시를 이름으로 골라 켜고 끄는데, 두건 쓴
  머리가 통째로 하나인 `Rogue_Head_Hooded` 가 "Hood" 때문에 장비로 걸려 백 비숍이 목 없이
  서 있었다. 벗길 수 있는 머리는 없다.
- 기물 색을 진영 색으로 물들이지 않는다. 캐릭터 고유색이 죽으면 캐릭터로 안 읽힌다 —
  진영은 실루엣과 발밑 원판으로 가른다.
- 렌더러를 둘로 나누지 않는다. 2D 유니코드 반상은 3D 도입과 함께 버렸다. 저사양 대응이
  필요해지면 3D 안에서 품질을 낮춘다.
- 전투 재생을 건너뛸 수 없게 만들지 않는다. 지금은 건너뛰기가 없지만, 넣는다면 재생만
  생략하고 판정은 그대로여야 한다 — 다시 굴리기는 절대 아니다.
- 전투 결과를 화면에서 계산하지 않는다. `combat.ts`가 낸 판정을 재생만 한다.
